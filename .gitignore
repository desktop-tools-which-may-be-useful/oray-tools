/target
/result
*.har
/tmp

# Local auth material: account name, password hash, tokens, client id.
# Only Cargo.toml belongs at the repository root — every other root-level
# toml (config.toml, 1.toml, ...) is a local config dump and must not be
# committed.
/*.toml
!/Cargo.toml
